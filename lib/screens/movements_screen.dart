import 'package:flutter/material.dart';
class MovementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is mov. screen'),
    );
  }
}